server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  # Angular routes (MUY IMPORTANTE)
  location / {
    try_files $uri $uri/ /index.html;
  }

  # cache assets
  location /assets/ {
    expires 1y;
    add_header Cache-Control "public, immutable";
  }
}